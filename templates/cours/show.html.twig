{% extends 'base.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
<div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Reservation info </h1>    <p class="create"> <a href="{{ path('app_cours_index') }}" class="btn btn-primary"  style="margin-bottom:10px;     width: 100%;">Retour a la liste </a>
 </p>  </div>
 <div class="card" style="width: 18rem;margin-left: 15px;">
  <img src="{{ asset(''~cour.image) }}" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{ cour.titre }}</h5>
    <p class="card-text">Contenu : {{ cour.contenu }}</p>
    <p class="card-text">Type du Cours : {{ cour.type }}</p>
    <a href="{{ path('exerciceidc', {'idCrs': cour.idCrs}) }}" class="btn btn-primary"  style="margin-bottom:10px;     width: 100%;">Afficher les Exercices </a>
    <a href="{{ path('app_cours_edit', {'idCrs': cour.idCrs}) }}" class="btn btn-success" style="margin-bottom:10px;width: 100%;">Modifier</a>
     {{ include('cours/_delete_form.html.twig') }}
  </div>
</div>
     </div>

    </div>
    </div>
{% endblock %}
