{% extends 'base.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
<div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Reservation info </h1>    <p class="create">  <a href="{{ path('app_exercices_index') }}">back to list</a>
 </p>  </div>
    <table class="table">
        <tbody>
          
            <tr>
                <th>Type</th>
                <td>{{ cour.type }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ cour.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ cour.contenu }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('uploads/'~cour.image) }}"width="150"height="120"alt="" ></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cours_index') }}">back to list</a>

    <a href="{{ path('app_cours_edit', {'idCrs': cour.idCrs}) }}">edit</a>

    {{ include('cours/_delete_form.html.twig') }}
     </div>

    </div>
    </div>
{% endblock %}
