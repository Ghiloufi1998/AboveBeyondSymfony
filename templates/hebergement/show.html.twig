{% extends 'base.html.twig' %}

{% block title %}Hebergement{% endblock %}

{% block body %}
    <div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Hebergement info </h1>    <p class="create">  <a href="{{ path('app_hebergement_index') }}">back to list</a>
 </p>  </div>

    <table class="table">
        <tbody>
            <tr>
                <th>HebergementId</th>
                <td>{{ hebergement.hebergementId }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ hebergement.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hebergement.type }}</td>
            </tr>
            <tr>
                <th>Disponibilité</th>
                <td>{{ hebergement.Disponibilite }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ hebergement.adresse }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ hebergement.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ hebergement.prix }}</td>
            </tr>
        </tbody>
    </table>

     <div class="buttons">

    <a class ="bton" href="{{ path('app_hebergement_edit', {'hebergementId': hebergement.hebergementId}) }}">edit</a>

    {{ include('hebergement/_delete_form.html.twig') }}
    </div>

    </div>
    </div>
{% endblock %}
