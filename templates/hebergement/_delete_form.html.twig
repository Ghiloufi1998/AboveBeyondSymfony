<form method="post" action="{{ path('app_hebergement_delete', {'hebergementId': hebergement.hebergementId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.hebergementId) }}">
    <button class="bton">Delete</button>
</form>
