{% extends 'base.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}
<div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Question info </h1>    <p class="create">   <a href="{{ path('app_questions_index') }}">back to list</a> </p>  </div>
    

    <table class="table">
        <tbody>
            <tr>
                <th>QuestionId</th>
                <td>{{ question.questionId }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ question.type }}</td>
            </tr>
        </tbody>
    </table>
     <div class="buttons">
    <a class ="bton" href="{{ path('app_questions_edit', {'questionId': question.questionId}) }}">edit</a>

    {{ include('questions/_delete_form.html.twig') }}
    </div>

    </div>
    </div>
    
{% endblock %}
