<form method="post" action="{{ path('app_guide_delete', {'idG': guide.idG}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide.idG) }}">
    <button class="bton">Delete</button>
</form>
