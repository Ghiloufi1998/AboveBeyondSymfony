<form method="post" action="{{ path('app_reservation_delete', {'revId': reservation.revId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.revId) }}">
    <button class="bton">Delete</button>
</form>
