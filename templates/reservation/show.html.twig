{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}

    <div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Reservation info </h1>    <p class="create">  <a href="{{ path('app_reservation_index') }}">back to list</a>
 </p>  </div>

    <table class="table">
        <tbody>
            <tr>
                <th>RevId</th>
                <td>{{ reservation.revId }}</td>
            </tr>
            <tr>
                <th>DateDeb</th>
                <td>{{ reservation.dateDeb ? reservation.dateDeb|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ reservation.dateFin ? reservation.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reservation.type }}</td>
            </tr>
            <tr>
                <th>NbrAdultes</th>
                <td>{{ reservation.nbrAdultes }}</td>
            </tr>
            <tr>
                <th>NbrEnfants</th>
                <td>{{ reservation.nbrEnfants }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ reservation.destination }}</td>
            </tr>
            <tr>
                <th>HebergementId</th>
                <td>{{ reservation.hebergementId }}</td>
            </tr>
            <tr>
                <th>VolId</th>
                <td>{{ reservation.volId }}</td>
            </tr>
        </tbody>
    </table>

   <div class="buttons">
    <a class ="bton" href="{{ path('app_reservation_edit', {'revId': reservation.revId}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
    </div>

    </div>
    </div>
{% endblock %}
