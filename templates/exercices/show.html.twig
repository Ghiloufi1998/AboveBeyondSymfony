{% extends 'base.html.twig' %}

{% block title %}Exercices{% endblock %}

{% block body %}
<div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Reservation info </h1>    <p class="create">  <a href="{{ path('app_exercices_index') }}">back to list</a>
 </p>  </div>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEx</th>
                <td>{{ exercice.idEx }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ exercice.type }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ exercice.question }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ exercice.reponse }}</td>
            </tr>
            <tr>
                <th>Hint</th>
                <td>{{ exercice.hint }}</td>
            </tr>
        </tbody>
    </table>

    
 <div class="buttons">
 
<a class ="bton" href="{{ path('app_exercices_edit', {'idEx': exercice.idEx}) }}">edit</a>

    {{ include('exercices/_delete_form.html.twig') }}
     </div>

    </div>
    </div>
{% endblock %}
