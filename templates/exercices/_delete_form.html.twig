<form method="post" action="{{ path('app_exercices_delete', {'idEx': exercice.idEx}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.idEx) }}">
       <button class="btn btn-danger">Supprimer</button>
</form>
