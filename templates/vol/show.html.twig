{% extends 'base.html.twig' %}

{% block title %}Vol{% endblock %}

{% block body %}

    
<div class="body">  
<div class="tab">
<div class="tab-header" > <h1> Liste des Vols </h1>    <p class="create">   <a href="{{ path('app_vol_new') }}">Create new</a></p>  </div>
 
    <table class="table">
        <tbody>
        
            <tr>
                <th>Destination</th>
                <td>{{ vol.destination }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ vol.depart }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ vol.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ vol.prix }}</td>
            </tr>
            <tr>
                <th>X</th>
                <td>{{ vol.x }}</td>
            </tr>
            <tr>
                <th>Y</th>
                <td>{{ vol.y }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vol_index') }}">back to list</a>

    <a href="{{ path('app_vol_edit', {'volId': vol.volId}) }}">edit</a>

    {{ include('vol/_delete_form.html.twig') }}
{% endblock %}
