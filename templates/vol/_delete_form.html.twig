<form method="post" action="{{ path('app_vol_delete', {'volId': vol.volId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vol.volId) }}">
    <button class="btn">Delete</button>
</form>
