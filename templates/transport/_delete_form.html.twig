<form method="post" action="{{ path('app_transport_delete', {'transportId': transport.transportId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transport.transportId) }}">
    <button class="bton">Delete</button>
</form>
