<form method="post" action="{{ path('app_reponses_delete', {'reponsesId': reponse.reponsesId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.reponsesId) }}">
    <button class="bton">Delete</button>
</form>
