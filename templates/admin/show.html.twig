{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}



{% block body %}
    <div class="body">  
    <div class="tab">
    
    <div class="tab-header" > <h1> Admin info </h1>    <p class="create">  <a href="{{ path('admin_index') }}">back to list</a>

    

    <table class="table">
        <tbody>
            
            <tr>
                <th>Adminname</th>
                <td>{{ admin.adminname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ admin.lastname }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ admin.birthday|date("m/d/y") }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ admin.gender }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ admin.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ admin.password }}</td>
            </tr>
            <a href="{{ path('admin_show', {'id': admin.id}) }}"><img src="{{ asset ('uploads/' ~ admin.image) }}" alt=""height="80px" width="80px"></a>
<br></br>
        </tbody>
    </table>


    <div class="buttons">

      <a class ="bton" href="{{ path('admin_edit', {'id': admin.id}) }}">edit</a>

   
      {{ include('admin/_delete_form.html.twig') }}
    </div>

    </div>
    </div>
{% endblock %}

    


