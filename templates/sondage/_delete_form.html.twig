<form method="post" action="{{ path('app_sondage_delete', {'sondageId': sondage.sondageId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sondage.sondageId) }}">
    <button class="bton">Delete</button>
</form>
