<!DOCTYPE html>

<title>Sondage</title>

{% block body %}
    <div class="body">  
    <div class="tab">
    <div class="tab-header" > <h1> Question info </h1>    <p class="create">  <a href="{{ path('app_sondage_index') }}">back to list</a>
 </p>  </div>

    <table class="table">
        <tbody>
            <tr>
                <th>SondageId</th>
                <td>{{ sondage.sondageId }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ sondage.sujet }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ sondage.categorie }}</td>
            </tr>
        </tbody>
    </table>
    <div class="buttons">
    <a href="{{ path('app_sondage_edit', {'sondageId': sondage.sondageId}) }}">edit</a>

    {{ include('sondage/_delete_form.html.twig') }}
    </div>

    </div>
    </div>
    
{% endblock %}
