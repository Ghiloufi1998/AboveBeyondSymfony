{% extends 'base.html.twig' %}

{% block body %}
    <div class="body-form" >
    <div  class="form">
        <h1>Edit Sondage</h1>

    {{ include('sondage/_form.html.twig', {'button_label': 'Update'}) }}
<br>
    <a href="{{ path('app_sondage_index') }}"><button type="button" class="btn btn-primary">back to list</button></a>
<br>
    {{ include('sondage/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
