<form method="post" action="{{ path('app_voyageorganise_delete', {'voyageId': voyageorganise.voyageId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyageorganise.voyageId) }}">
    <button class="btn">Delete</button>
</form>
