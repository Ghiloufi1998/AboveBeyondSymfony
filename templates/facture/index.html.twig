{% extends 'base.html.twig' %}
  {% block body %}
  <div class="body">  
<div class="tab">
<div class="tab-header" > <h1>  Facture index </h1>    <p class="create"> <a href="{{ path('app_facture_new') }}">Create new</a> </p>  </div>
                  <table class="table">
                     <thead>
                      <tr>
                       <th>Numero Facture</th>
                       <th>Date echance</th>
                       <th>Montant</th>
                       <th>Etat</th>
                       <th>Numero Paiment</th>
                       <th>Numero Reservation</th>
                       <th>actions<th>
                      </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                            {% for facture in factures %}
                      <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>{{ facture.getIdFac() }}</strong></td>
                       <td>{{ facture.DateEch ? facture.DateEch |date('Y-m-d') : '' }}</td>
                       <td>{{ facture.getMontantTtc() }}</td>
                       <td>{{ facture.getEtat() }}</td>
                       <td ><a href="{{ path('app_paiement_show', {'paiId': facture.getPai().paiId  }) }}">{{ facture.getPai().paiId}}</a> </td>
                     <td ><a href="{{ path('app_reservation_show', {'revId': facture.getRev().revId  }) }}">{{ facture.getRev().revId }}</a> </td> 
                        <td>
                          <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                              <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                          
                              <a class="dropdown-item" href="{{ path('app_facture_show', {'idFac': facture.idFac}) }}" href="javascript:void(0);""  >
                                <i class="bx bx-edit-alt me-1"></i> show</a>
                              
                              <a class="dropdown-item" href="{{ path('app_facture_edit', {'idFac': facture.idFac}) }}" href="javascript:void(0);"">
                                <i class="bx bx-trash me-1"></i> Delete</a>
                              
                            </div>
                          </div>
                        </td>
                      </tr>
                        {% else %}
                             <tr>
                               <td colspan="5">no records found</td>
                            </tr>
                     {% endfor %}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
            <br>
            <br>
    
         {% endblock %} 
            