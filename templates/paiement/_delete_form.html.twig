<form method="post" action="{{ path('app_paiement_delete', {'paiId': paiement.paiId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.paiId) }}">
      <button class="bton">Delete</button>
</form>
