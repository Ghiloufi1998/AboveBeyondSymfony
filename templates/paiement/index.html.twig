 {% extends 'base.html.twig' %}
 {% block body %}
   <div class="body">  
<div class="tab">
<div class="tab-header" > <h1>Paiement index</h1>   <p class="create"> <a href="{{ path('app_paiement_new') }}">Create new</a> </p>  </div>
             <table class="table">
                 <thead>
                  <tr>
                <th>PaiId</th>
                <th>Date</th>
                <th>Montant</th>
                <th>ModePay</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paiement in paiements %}
            <tr>
                <td>{{ paiement.getPaiId() }}</td>
                <td>{{ paiement.getDate() ? paiement.getDate()|date('Y-m-d') : '' }}</td>
                <td>{{ paiement.getMontant() }}</td>
                <td>{{ paiement.getModePay() }}</td>
                <td>
                 <td>
                          <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                              <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                          
                              <a class="dropdown-item" href="{{ path('app_paiement_show', {'paiId': paiement.paiId}) }}" href="javascript:void(0);""  >
                                <i class="bx bx-edit-alt me-1"></i> show</a>
                              
                              <a class="dropdown-item" href="{{ path('app_paiement_edit', {'paiId': paiement.paiId}) }}"href="javascript:void(0);"">
                                <i class="bx bx-trash me-1"></i> Delete</a>
                              
                            </div>
                          </div>
                        
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_paiement_new') }}">Create new</a>
{% endblock %}
    
