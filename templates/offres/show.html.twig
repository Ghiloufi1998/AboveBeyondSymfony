{% extends 'base.html.twig' %}

{% block title %}Offres{% endblock %}

{% block body %}
<div class="body">  
<div class="tab">
<div class="tab-header" >   <h1>Offres</h1>   <p class="create"> <a href="{{ path('app_offres_index') }}">back to list</a> </p>  </div>
  
   

    <table class="table">
        <tbody>
            <tr>
                <th>IdOff</th>
                <td>{{ offre.idOff }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
            <tr>
                <th>NbPointReq</th>
                <td>{{ offre.nbPointReq }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ offre.destination }}</td>
            </tr>
            <tr>
                <th>PourcentageRed</th>
                <td>{{ offre.pourcentageRed }}</td>
            </tr>
        </tbody>
    </table>

   
<div class="buttons">
    <a class ="bton" href="{{ path('app_offres_edit', {'idOff': offre.idOff}) }}">edit</a>

    {{ include('offres/_delete_form.html.twig') }}
    </div>
{% endblock %}
