
<form method="post" action="{{ path('app_offres_delete', {'idOff': offre.idOff}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.idOff) }}">
    <button class="bton">Delete</button>
</form>
